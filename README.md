install the following to get LSPs to work

go install golang.org/x/tools/gopls@latest

npm install -g pyright

npm install -g typescript typescript-language-server

rustup component add rust-analyzer

